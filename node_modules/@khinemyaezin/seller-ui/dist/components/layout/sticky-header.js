import { jsx as _jsx } from "react/jsx-runtime";
import { useScrolled } from "../../hooks/use-scrolled";
export default function StickyHeader({ children }) {
    const isScrolled = useScrolled();
    return (_jsx("div", { className: `flex items-center justify-between sticky top-0 z-10 transition-all ${isScrolled
            ? "pt-6 bg-white/80 dark:bg-zinc-900/80 backdrop-blur-md shadow-sm"
            : ""}`, children: children }));
}
//# sourceMappingURL=sticky-header.js.map