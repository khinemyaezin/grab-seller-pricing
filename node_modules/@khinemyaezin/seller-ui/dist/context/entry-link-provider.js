import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useContext } from "react";
const EntryLinkContext = createContext(null);
function EntryLinkProvider({ link, children }) {
    return (_jsx(EntryLinkContext.Provider, { value: link, children: children }));
}
function useEntryLink() {
    const context = useContext(EntryLinkContext);
    if (!context) {
        throw new Error("Entry link must be used within an LinkProvider");
    }
    return context;
}
export { EntryLinkProvider, useEntryLink };
//# sourceMappingURL=entry-link-provider.js.map